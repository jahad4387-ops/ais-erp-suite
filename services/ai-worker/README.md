# AI Worker

## 职责

- OCR 与票据结构化。
- 大模型调用与提示词版本管理。
- 凭证草稿、核销建议、结账检查、报表解读。
- Agent dry-run 执行、风险评估、证据链整理。

## 不负责

- 不直接记账。
- 不直接付款。
- 不直接关闭会计期间。
- 不绕过业务 API 修改数据库。

## 下一步

1. 定义 `AiSuggestion`、`EvidenceRef`、`AgentAction` 数据结构。
2. 接入旧项目中的 OCR 与模型供应商配置能力。
3. 为 AI 记账草稿建立评估集：正确科目、税率、辅助核算、摘要、借贷方向。
