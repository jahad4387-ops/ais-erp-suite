# Desktop App

## 职责

- 作为可选桌面端入口，承接离线、局域网、小企业安装包等场景。
- 复用或迁移 `C:\antigra-workplace\ai-finance-desktop` 的 OCR、凭证、报表、审批和桌面打包能力。

## 不负责

- 不作为完整 ERP 的唯一实现位置。
- 不复制旧项目的临时数据结构到新系统核心。
- 不在桌面端绕过服务端审计。

## 下一步

1. 盘点旧项目可迁移能力。
2. 将 OCR 和 AI 记账能力抽象为 `services/ai-worker` 接口。
3. 将凭证纸和报表导出能力抽象为共享 UI/导出模块。
